\documentclass[10pt]{article}
\usepackage[margin=0.7in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb}
\usepackage{enumitem}
\setlist[enumerate]{leftmargin=*, itemsep=7mm}
\setlist[enumerate,2]{label=(\Alph*), leftmargin=2em, itemsep=0.05em, topsep=0.25em}
\pagestyle{plain}

\begin{document}
\begin{center}
  {\large\bfseries Video 2 Quiz: Predicate Logic}\\[0.25em]
  \textit{Choose the best answer for each question.}
\end{center}
\noindent Name:\ \rule{3.2in}{0.4pt}\hfill Date:\ \rule{1.25in}{0.4pt}
\vspace{0.7em}
\begin{enumerate}
  \item During the discussion of negated quantified statements, what does the video invite students to pause and try?
  \begin{enumerate}
    \item Drawing a Venn diagram for the color collection.
    \item Listing every vehicle with four tires.
    \item Rephrasing four associated statements as English sentences.
    \item Constructing a truth table with three variables.
  \end{enumerate}

  \item Which inference does the video illustrate with an image of a rainbow?
  \begin{enumerate}
    \item $\bigl(\forall x\in I,\ \forall y\in C:\ P(x,y)\bigr)\Rightarrow\bigl(\forall y\in C,\ \exists x\in I:\ P(x,y)\bigr)$.
    \item $\bigl(\exists x\in I,\ \forall y\in C:\ P(x,y)\bigr)\Rightarrow\bigl(\forall y\in C,\ \exists x\in I:\ P(x,y)\bigr)$.
    \item $\bigl(\forall y\in C,\ \exists x\in I:\ P(x,y)\bigr)\Rightarrow\bigl(\exists x\in I,\ \forall y\in C:\ P(x,y)\bigr)$.
    \item $\bigl(\exists x\in I,\ \exists y\in C:\ P(x,y)\bigr)\Rightarrow\bigl(\forall x\in I,\ \forall y\in C:\ P(x,y)\bigr)$.
  \end{enumerate}

  \item Which example does the video say will be presented in the live session?
  \begin{enumerate}
    \item A truth-table proof that every conditional is a tautology.
    \item Euclid's proof that if $x^2=2$ and $x$ is real, then $x$ is not rational.
    \item The vehicle example showing that all vehicles have four tires.
    \item The claim that every image contains every color.
  \end{enumerate}

  \item For what part of this class do the slides suggest that intuition will be useful?
  \begin{enumerate}
    \item Logic.
    \item Quantifiers.
    \item Negation.
    \item Proof.
  \end{enumerate}
\end{enumerate}
\end{document}
